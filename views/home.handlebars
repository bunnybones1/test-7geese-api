<h1>Welcome</h1>
<div class="container">
	<p><b>{{username}}</b></p>
	<p>{{access_token}}</p>
	<p><a href="/logout">logout</a></p>
</div>
<p>
    {{#each users}}
    <img src="{{this.face_img}}" title="{{this.first_name}} {{this.last_name}}">
    {{/each}}
</p>
<p>{{faceTest}}</p>
